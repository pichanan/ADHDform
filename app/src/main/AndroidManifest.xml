<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="adhd.sirikan.pimpicha.adhdform">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".AddChildActivity" />
        <activity android:name=".SnapActivity" />
        <activity android:name=".TassActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".BeforeTestActivity" />
        <activity android:name=".TreeTestActivity" />
        <activity android:name=".SdqActivity" />
        <activity android:name=".GraphActivity" />
        <activity android:name=".ShowTwoTable" />
        <activity android:name=".ShowChildDetail"></activity>
    </application>

</manifest>